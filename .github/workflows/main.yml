name: Docker Push

on: push

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Docker Build & Push to GitHub Package
        uses: opspresso/action-docker@master
        with:
          args: --docker
        env:
          USERNAME: ${{ secrets.GHTOKEN }}
          PASSWORD: ${{ secrets.GHUSERNAME }}
          REGISTRY: "docker.pkg.github.com/marlluslustosa/docker-to-onion"
          DOCKERFILE: "Dockerfile"
          IMAGE_NAME: "docker-to-onion"
          TAG_NAME: "v0.0.1"
          LATEST: "true"
